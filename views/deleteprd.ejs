<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eliminar Producto</title>
        <!-- 
    - favicon
  -->
  <link rel="shortcut icon" href="https://img.freepik.com/vector-premium/icono-camara-minimalista-e-iconico_313877-12100.jpg" type="">

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f1f1f1;
        }

        .container {
            max-width: 400px;
            padding: 20px;
            background-color: white;
            border: 2px solid rgb(149, 47, 218);
            border-radius: 12px;
            text-align: center;
        }

        h4,h3 {
            border-radius: 5px;
            padding: 10px;
            margin: 10px 0;
        }
        img {
            max-width: 100%;
            margin: 10px 0;
        }

        button {
            padding: 10px 20px;
            background-color: rgb(100, 9, 173);
            color: white;
            border: none;
            border-radius: 5px;
            outline: none;
            cursor: pointer;
        }

        button:hover {
            background-color: rgb(66, 15, 161);
        }
        .return-button {
			position: fixed;
			top: 20px;
			right: 20px;
			padding: 10px 20px;
			background-color: rgb(140, 19, 170);
			color: white;
			border: none;
			border-radius: 4px;
			font-size: 16px;
			cursor: pointer;
		}

		.return-button:hover {
			background-color: rgb(138, 21, 184);
		}
    </style>

</head>
<body>
    <div class="container">
        <div>
            <a class="nav-link return-button" href="/logout">Salir.</a>
        </div>
        <div class="header">
        <h2>¿Está seguro de eliminar este producto?</h2>
        </div>
        <hr>
        <form action="/deleteprd/<%= productos.id %>" method="get">
            <h4><%= productos.nombre %></h4>
            <h4>Codigo :<hr><%= productos.codigo %></h4>
            <h4>Precio :<hr><%= productos.precio %></h4>
            <h4>Marca :<hr><%= productos.marca %></h4>
            <h4>Jugadores :<hr><%= productos.jugadores %></h4>
            <h4>Descripcion :<hr><%= productos.descripcion %></h4>
            <div style="margin-top: 10px; margin-bottom: 10px;"><button type="submit" class="btn btn-primary">Eliminar Producto</button></div>
        </form>
    </div>
</body>
</html>
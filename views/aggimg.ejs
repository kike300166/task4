<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Agregar Imagenes</title>
	    <!-- 
    - favicon
  -->
  <link rel="shortcut icon" href="https://img.freepik.com/vector-premium/icono-camara-minimalista-e-iconico_313877-12100.jpg" type="">



	<style>
		@import url('https://fonts.googleapis.com/css?family=Muli&display=swap');
		@import url('https://fonts.googleapis.com/css?family=Open+Sans:400,500&display=swap');

		* {
			box-sizing: border-box;
		}

		body {
			background-color: #8eebe3d5;
			font-family: 'Open Sans', sans-serif;
			display: flex;
			align-items: center;
			justify-content: center;
			min-height: 100vh;
			margin: 0;
		}

		.container {
			background-color: #fff;
			border-radius: 5px;
			box-shadow: 0 2px 20px 0px #03153b2e;
			overflow: hidden;
			width: 400px;
			max-width: 100%;
		}

		.header {
			border-bottom: 1px solid #f0f0f0;
			background-color: #ccd8ef;
			padding: 20px 40px;
		}

		.header h2 {
			margin: 0;
			color: #ab51e7;
		}

		.form {
			padding: 30px 40px;
			font-weight: 500;
			color: #d577ec;
		}

		.form-control {
			margin-bottom: 10px;
			padding-bottom: 20px;
			position: relative;
		}

		.form-control label {
			display: inline-block;
			margin-bottom: 5px;
		}

		.form-control input {
			border: 2px solid #f0f0f0;
			border-radius: 4px;
			display: block;
			font-family: inherit;
			font-size: 14px;
			padding: 10px;
			width: 100%;
		}

		.form-control input:focus {
			outline: 0;
			border-color: #eceaec;
		}

		.form-control.success input {
			border-color: #78c096d8;
		}

		.form-control.error input {
			border-color: #ce2916;
		}

		.form-control i {
			visibility: hidden;
			position: absolute;
			top: 40px;
			right: 10px;
		}

		.form-control.success i.fa-check-circle {
			color: #5cd1ee;
			visibility: visible;
		}

		.form-control.error i.fa-exclamation-circle {
			color: #da7add;
			visibility: visible;
		}

		.form-control small {
			color: #dd8dc9;
			position: absolute;
			bottom: 0;
			left: 0;
			visibility: hidden;
		}

		.form-control.error small {
			visibility: visible;
		}

		.form button {
			background-color: #da88ccd5;
			border: 2px solid #d36bced2;
			border-radius: 4px;
			color: #fff;
			display: block;
			font-family: inherit;
			font-size: 16px;
			padding: 10px;
			margin-top: 20px;
			width: 100%;
			cursor: pointer;
			transition: 0.6s;
		}

		.form button:hover {
			transform: scale(0.95);
			opacity: .8;
		}

		input::placeholder {
			color: #b342cfda;
		}



		form {
			display: flex;
			flex-direction: column;
			align-items: center;
		}

		select {
			width: 100%;
			padding: 10px;
			margin-bottom: 10px;
			color: rgb(182, 72, 204);
			border: 1px solid rgba(178, 80, 216, 0.87);
			outline: none;
		}

		button {
			width: 100%;
			padding: 10px;
			margin-top: 10px;
			color: white;
			background-color: rgb(176, 89, 216);
			border: none;
			outline: none;
			cursor: pointer;
		}

		button:hover {
			background-color: rgba(223, 120, 236, 0.767);
		}

		.return-button {
			position: fixed;
			top: 20px;
			right: 20px;
			padding: 10px 20px;
			background-color: rgb(193, 101, 211);
			color: white;
			border: none;
			border-radius: 4px;
			font-size: 16px;
			cursor: pointer;
		}

		.return-button:hover {
			background-color: rgba(212, 100, 212, 0.822);
		}
	</style>

</head>

<body>

	<div class="container">
		<div>
			<a class="nav-link return-button" href="/logout">Salir.</a>
		</div>


		<div class="header">
			<h2>Agregar Imagenes</h2>
		</div>


		<form action="/aggimg" method="post" class="form">
			<div class="form-control">
				<label>Agrege la imagen</label>
				<input type="text" name="url" id="url" placeholder="Ingrese la url de la imagen" required
					maxlength="200" minlength="3" />
				<i class="fas fa-check-circle"></i>
				<i class="fas fa-exclamation-circle"></i>
				<small>Error message</small>
			</div>

			<select name="destacado" id="destacado">
				<option value="">꧁♥𓆏 A̼r̼e̼ y̼o̼u̼ r̼e̼a̼d̼y̼? 𓆏♥꧂</option>

				<option value="Si">yes</option>
				<option value="No">NO</option>
			</select>

			<select name="producto_id" id="producto_id">
				<option value="">Elija una opcion</option>
				<% if (productos) { %>
					<% for (let i=0; i < productos.length; i++){ %>
						<% if (productos) { %>
							<option value="<%= productos[i].id %>" id="producto_id" name="producto_id">
								<%= productos[i].nombre %>
							</option>
							<% } %>
								<% } %>
									<% } else { %>
										<option value="">No existen productos en el sistema</option>
										<% } %>
			</select>
			<button type="submit" class="btn btn-primary">Subir Imagen</button>
		</form>
	</div>

</body>

</html>
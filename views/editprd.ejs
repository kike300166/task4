<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Editar Producto</title>
        <!-- 
    - favicon
  -->
  <link rel="shortcut icon" href="https://img.freepik.com/vector-premium/icono-camara-minimalista-e-iconico_313877-12100.jpg" type="">

    <style>
        @import url('https://fonts.googleapis.com/css?family=Muli&display=swap');
        @import url('https://fonts.googleapis.com/css?family=Open+Sans:400,500&display=swap');

        * {
            box-sizing: border-box;
        }

        body {
            background-color: #8325cf;
            font-family: 'Open Sans', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 2px 20px 0px #03153b2e;
            overflow: hidden;
            width: 400px;
            max-width: 100%;
        }

        .header {
            border-bottom: 1px solid #f0f0f0;
            background-color: #ccd8ef;
            padding: 20px 40px;
        }

        .header h2 {
            margin: 0;
            color: #212C42;
        }

        .form {
            padding: 30px 40px;
            font-weight: 500;
            color: #03153b;
        }

        .form-control {
            margin-bottom: 10px;
            padding-bottom: 20px;
            position: relative;
        }

        .form-control label {
            display: inline-block;
            margin-bottom: 5px;
        }

        .form-control input {
            border: 2px solid #f0f0f0;
            border-radius: 4px;
            display: block;
            font-family: inherit;
            font-size: 14px;
            padding: 10px;
            width: 100%;
        }

        .form-control input:focus {
            outline: 0;
            border-color: #777;
        }

        .form-control.success input {
            border-color: #780d8d;
        }

        .form-control.error input {
            border-color: #4037be;
        }

        .form-control i {
            visibility: hidden;
            position: absolute;
            top: 40px;
            right: 10px;
        }

        .form-control.success i.fa-check-circle {
            color: #b131bd;
            visibility: visible;
        }

        .form-control.error i.fa-exclamation-circle {
            color: #df3e2c;
            visibility: visible;
        }

        .form-control small {
            color: #e7a39c;
            position: absolute;
            bottom: 0;
            left: 0;
            visibility: hidden;
        }

        .form-control.error small {
            visibility: visible;
        }

        .form button {
            background-color: #ce1ab0;
            border: 2px solid #d60d93;
            border-radius: 4px;
            color: #fff;
            display: block;
            font-family: inherit;
            font-size: 16px;
            padding: 10px;
            margin-top: 20px;
            width: 100%;
            cursor: pointer;
            transition: 0.6s;
        }

        .form button:hover {
            transform: scale(0.95);
            opacity: .8;
        }

        input::placeholder {
            color: #1d346565;
        }




        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        select {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            color: rgb(123, 34, 182);
            border: 1px solid rgb(175, 68, 218);
            outline: none;
        }

        button {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            color: white;
            background-color: rgb(152, 69, 219);
            border: none;
            outline: none;
            cursor: pointer;
        }

        button:hover {
            background-color: rgb(150, 46, 190);
        }


        .return-button {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 10px 20px;
            background-color: rgb(197, 62, 224);
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

        .return-button:hover {
            background-color: rgb(156, 32, 150);
        }
    </style>

</head>
<body>
    <div class="container" >
        <div>
            <a class="nav-link return-button" href="/logout">Salir.</a>
        </div>

        <div class="header">
			<h2>Actualizar Producto</h2>
		</div>
        <hr>
        <form action="/updateprd/<%= productos.id %>" method="post">
            <div style="margin-top: 10px; margin-bottom: 10px;"><p>Nombre :<hr></p><input type="text" name="nombre" id="nombre" value="<%= productos.nombre %>" required maxlength="25" minlength="3"></div>
            <div style="margin-top: 10px; margin-bottom: 10px;"><p>Codigo :<hr></p><input type="number" name="codigo" id="codigo" value="<%= productos.codigo %>" required></div>
            <div style="margin-top: 10px; margin-bottom: 10px;"><p>Precio :<hr></p><input type="number" name="precio" id="precio" value="<%= productos.precio %>" required></div>
            <div style="margin-top: 10px; margin-bottom: 10px;"><p>Marca :<hr></p><input type="text" name="marca" id="marca" value="<%= productos.marca %>" required maxlength="25" minlength="4"></div>
            <div style="margin-top: 10px; margin-bottom: 10px;"><p>Jugadores :<hr></p><input type="number" name="jugadores" id="jugadores" value="<%= productos.jugadores %>" required></div>
            <div style="margin-top: 10px; margin-bottom: 10px;"><p>Descripcion :<hr></p><input type="textarea" name="descripcion" id="descripcion" value="<%= productos.descripcion %>" maxlength="60" minlength="10"></div>
            <div style="margin-top: 10px; margin-bottom: 10px;"><hr><button type="submit" class="btn btn-primary">Actualizar Producto</button></div>
        </form>
    </div>
</body>
</html>